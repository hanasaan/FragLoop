# Minimal runtime deps for Frag Loop (inference + WebUI)
# Install CUDA/MPS build of torch separately as needed.

torch
numpy
websockets>=12.0
moderngl
Pillow
huggingface_hub>=0.24.0
rustbpe>=0.1.0
tiktoken>=0.11.0
tokenizers>=0.22.0
websockets
